<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ACCESS_NETWORK_STATE is library-injected (Ktor's OkHttp engine + Coil
         use ConnectivityManager). It is normal-protection, auto-granted at
         install, not personally-identifying, and is acknowledged explicitly in
         CHANGELOG under Privacy so readers of the source manifest don't have
         to diff against the merged manifest to find it. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:name=".PantryTrackerApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PantryTracker"
        tools:targetApi="36">
        <!-- Cut off ML Kit's Firebase-DataTransport telemetry pipeline.
             ML Kit barcode-scanning transitively pulls in google-datatransport,
             which registers a `cct` backend via TransportBackendDiscovery and
             a JobScheduler that uploads queued events to
             firebaselogging.googleapis.com. The standard
             `firebase_data_collection_default_enabled=false` meta-data is only
             honored by FirebaseInitProvider (which standalone ML Kit doesn't
             use), so the flag does nothing here — verified empirically.
             Removing these three components leaves the barcode-detection
             pipeline intact (it doesn't depend on the transport) but means
             events queued to SQLiteEventStore have no registered backend to
             ship to, so nothing leaves the device. Required to keep the
             CHANGELOG's "single OFF lookup is the only outbound call" promise
             honest. -->
        <service
            android:name="com.google.android.datatransport.runtime.backends.TransportBackendDiscovery"
            tools:node="remove" />
        <service
            android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.JobInfoSchedulerService"
            tools:node="remove" />
        <receiver
            android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.AlarmManagerSchedulerBroadcastReceiver"
            tools:node="remove" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
